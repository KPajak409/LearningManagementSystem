@page
@model LMS.Pages.Sections.EditSectionModel

@{
    ViewData["Title"] = "EditSection";
}

<h1>EditSection</h1>

<h4>Section</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="Section.Id" />
            <div class="form-group">
                <label asp-for="Section.Position" class="control-label"></label>
                <input asp-for="Section.Position" class="form-control" />
                <span asp-validation-for="Section.Position" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Section.Title" class="control-label"></label>
                <input asp-for="Section.Title" class="form-control" />
                <span asp-validation-for="Section.Title" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Section.CourseId" class="control-label"></label>
                <select asp-for="Section.CourseId" class="form-control" asp-items="ViewBag.CourseId"></select>
                <span asp-validation-for="Section.CourseId" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-page="./Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
